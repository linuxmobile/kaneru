---
import type { NavLink } from "~/types";

interface Props {
	link: NavLink;
}

const { link } = Astro.props;
---

<a
  class="px-3 underline"
  rel={link.external ? 'noopener noreferrer' : undefined}
  target={link.external ? '_blank' : undefined}
  href={link.url}
>
  {link.name}
</a>
