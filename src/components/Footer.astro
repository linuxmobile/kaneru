---
import SocialLinks from "~/components/SocialLinks.astro";
import siteConfig from "~/site.config";
---

<footer
  class="mt-auto grow-0 flex flex-row items-center justify-between max-w-full text-xs text-background bg-accent"
>
  <div class="flex flex-col md:flex-row flex-wrap flex-1 items-center justify-start px-2">
    <span class="my-1">© Not really copyrighted, but we pretend it is.</span>
  </div>
  {siteConfig.socialLinks && <SocialLinks socialLinks={siteConfig.socialLinks} />}
</footer>
